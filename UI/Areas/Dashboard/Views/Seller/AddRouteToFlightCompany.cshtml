@model FlightCompany 
@{
    Layout = "~/Areas/Dashboard/Views/Shared/_DashboardLayout.cshtml";
}

<div class="card col-lg-6 mx-auto">
    <div class="card-header">
        <div class="row">
            <div class="col-md-9">
                <h4>Add Route to @Model.FlightCompanyName</h4>
            </div>
            <div class="col-md-3 text-right">
                <a href="@TempData["referer"]" data-toggle="tooltip" title="Go Back" data-placement="bottom">
                    <i class="fas fa-arrow-left fa-2x text-muted"></i>
                </a>
            </div>
        </div>
    </div>
    <div class="card-body">
        <div asp-validation-summary="All" class="text-danger">
        </div>
        <form asp-action="AddRouteToFlightCompany" method="post">
            @Html.AntiForgeryToken()
            <div class="form-group">
                <div class="row">
                    <div class="col-lg-3">
                        <label>Routes</label>
                    </div>
                    <div class="col-lg-9">
                        <select id="dropDownList" name="selectedIds" asp-items="@TempData["routes"] as SelectList" multiple class="form-control">
                        </select>
                    </div>
                </div>
            </div>
            <div class="col-lg-12">
                <button type="submit" class="btn btn-outline-primary mt-2 ">Add</button>
            </div>
        </form>
    </div>
</div>

@section scripts{

    <script>
        $(document).ready(function () {
            $('#dropDownList').multiselect();
            sessionStorage.setItem("parentid", "Sellers");
            sessionStorage.setItem("id", "FlightCompany");
        });
    </script>

}

